# Docker Compose 端口
FRONTEND_PORT=80
BACKEND_PORT=8000

# 允许直接访问 FastAPI 的浏览器来源，多个来源使用逗号分隔。
CORS_ORIGINS=http://localhost,http://127.0.0.1

# LLM 可选配置；不填写时仍可使用 MockProvider。
OPENAI_API_KEY=
OPENAI_MODEL=
DASHSCOPE_API_KEY=
TONGYI_MODEL=
ARK_API_KEY=
DOUBAO_MODEL=
